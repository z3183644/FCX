# 安全策略

FCX 会操作 EA Ultimate Team Web App、访问用户本机求解器，并可选连接 FCX 账号与小程序远程控制。安全问题可能导致球员误消耗、任务重复执行、账号会话泄露或本地资源被滥用，因此请负责任地报告问题。

## 支持范围

安全修复优先支持：

- 当前 GitHub Release 中的最新 `FCX.js`。
- 与最新用户脚本同时发布的 Windows `FCX后端.exe` 和 macOS `FCX后端.app` 压缩包。
- `main` 分支当前代码。

旧版本可能不再单独修复。提交报告前，请先确认问题能否在最新版本复现。

## 如何报告安全问题

请优先使用 GitHub 的 **Private vulnerability reporting / Security advisory** 私下报告，不要先创建公开 Issue。

如果仓库暂时没有开放私密安全报告，可通过 [FCX 官网](https://fczhushou.com) 提供的联系渠道通知维护者。首次联系只需说明影响范围和联系方式，不要公开发送 Token、Cookie、完整用户数据或可直接利用的攻击代码。

报告建议包含：

- 受影响版本和构建哈希。
- 操作系统、浏览器、Tampermonkey 和 EA Web App build。
- 问题类型和可能影响。
- 最小复现步骤。
- 实际结果与预期结果。
- 已脱敏的日志、截图或请求结构。
- 你是否已验证最新版本。
- 建议修复方向（可选）。

请不要提交：

- EA Cookie、EA Session、完整请求头。
- FCX Access Token 或 Refresh Token。
- 真实密码、邮箱、手机号或 persona ID。
- 完整俱乐部球员池。
- 其他用户的数据。

## 响应流程

维护者会尽力按照以下流程处理：

1. 确认收到报告并判断是否属于安全问题。
2. 在隔离环境中复现并评估影响。
3. 制定修复、兼容和回滚方案。
4. 先发布修复，再根据风险决定是否公开细节。
5. 经报告者同意，在公告中致谢。

项目由个人维护，无法承诺企业级 SLA。高影响、可稳定复现且可能造成资产损失的问题会优先处理。

## 安全边界

### 用户脚本

- 用户脚本是完整静态文件，不从 FCX 主站下载或执行远程 JavaScript。
- `version.json` 只包含版本和更新说明。
- `routines.json` 只允许经过白名单校验的声明式流程。
- FCX 不应读取或上传 EA Cookie、EA Token。
- 本地求解请求只发送到 `127.0.0.1` 配置端口。
- 球员池和求解请求不经远程 FCX API 中转。

### 账号与远控

- 登录密码只用于 HTTPS 登录请求，不应写入本地持久化存储或日志。
- Access Token、Refresh Token 和设备信息存放在 Tampermonkey GM 存储。
- 远程命令只能映射到脚本内置的固定命令类型，不允许执行远程代码。
- 小程序远控只用于状态、目录和命令；本地求解仍由用户电脑完成。

### 本地后端

- 后端只应监听 `127.0.0.1`。
- 不要把本地端口转发到公网，不要配置公网反向代理。
- 当前本地服务不是多用户隔离服务；同一台电脑上的其他进程可能访问该端口。
- 使用公共电脑时，应在离开前关闭本地后端并退出 FCX 账号。

### 本地数据

- 设置和保护规则存放在 EA 页面本地存储。
- 账号会话存放在 Tampermonkey GM 存储。
- 价格缓存和任务历史存放在浏览器 IndexedDB。
- 清除站点数据、Tampermonkey 数据或浏览器配置可能删除这些信息。

## 安全测试规则

允许：

- 在自己的账号、设备和数据上进行测试。
- 对本仓库代码做静态分析、依赖审计和本地模糊测试。
- 在不影响其他用户的前提下验证本地后端。

不允许：

- 测试、访问或修改其他用户账号和数据。
- 对 FCX 主站、EA 服务或第三方服务执行拒绝服务测试。
- 绕过付费、访问控制、限流或平台规则。
- 在修复发布前公开可直接利用的高风险细节。
- 使用自动化测试制造 EA 市场、SBC 或账号异常流量。

## 供应链与发布

- JavaScript 依赖由 `package-lock.json` 固定。
- GitHub Actions 使用干净环境执行测试和构建。
- Release 同时发布用户脚本、后端 EXE 和 SHA256 清单。
- 使用者应从项目正式 Release 或维护者公布的渠道下载，并核对哈希。
- 构建依赖更新后应重新执行 `npm audit`、测试和 EXE 冒烟测试。

## 非安全问题

以下情况通常应通过普通 Issue 或支持渠道报告：

- EA 更新后按钮消失。
- 某个 SBC 无法求解。
- 远程流程 ID 过期。
- 卡包、挑选或 UI 显示异常，但不涉及越权、泄露或资产误消耗。
- 功能建议和性能优化。

如果无法判断，请按安全问题私下报告，维护者会协助分类。

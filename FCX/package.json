{
  "name": "fcx-auto-sbc-frontend",
  "version": "26.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.19.0"
  },
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "build:bundle": "vite build",
    "build:greasyfork": "vite build --mode greasyfork && node scripts/verify-greasyfork-build.mjs",
    "build": "npm run typecheck && npm run build:bundle && node scripts/verify-build.mjs && node scripts/assemble-release.mjs",
    "check": "npm run typecheck && npm run test && npm run build:bundle && node scripts/verify-build.mjs && node scripts/assemble-release.mjs"
  },
  "devDependencies": {
    "@types/node": "26.1.2",
    "happy-dom": "20.11.1",
    "typescript": "7.0.2",
    "vite": "8.2.0",
    "vite-plugin-monkey": "8.1.0",
    "vitest": "4.1.10"
  }
}
